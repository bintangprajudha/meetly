<script setup lang="ts">
import { usePage, Link } from '@inertiajs/vue3';
import { computed } from 'vue';

const page = usePage();

// Definisikan tipe untuk user yang sedang login dan user profil
interface User {
  id: number;
  name: string;
  email?: string;
}

</script>

<template>
  <div class="flex flex-col min-h-screen">
    <header class="flex items-center justify-between py-3 px-6 bg-white shadow-sm border-b border-gray-200">
          <div class="flex items-center space-x-4">
            <h1 class="text-xl font-semibold text-gray-900">Meetly</h1>
          </div>

          <div class="flex items-center space-x-4">
            <!-- <div class="relative" v-if="page.component === 'Dashboard'">
              <input type="text" placeholder="Search..." class="w-64 px-4 py-2 pl-10 pr-4 text-gray-700 bg-gray-50 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500" />
              <svg class="absolute left-3 top-2.5 w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
              </svg>
            </div>  -->
          </div>

    </header>

    <div class="flex-1 overflow-auto">
      <slot />
    </div>
  </div>
</template>